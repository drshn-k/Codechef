#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long cas;
    cin >> cas;

    while (cas--)
    {
        int x, y, z;
        cin >> x >> y >> z ;

        int m = x * 4 + y * 2;

        cout << m << endl;
    }

    return 0;
}